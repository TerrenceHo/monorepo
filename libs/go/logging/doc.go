/*
Package logging is a wrapper around Uber's zap logger, which has many
configuration possibilities, so this package simplifies the instantiation of the
logger. Services can still create their own zap logger.
*/
package logging
